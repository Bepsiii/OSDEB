discord.py>=2.3.0  # Or your specific version
google-generativeai>=0.3.0 # For Gemini integration
yt-dlp>=2023.07.06 # For the music cog (youtube-dl fork)
PyNaCl>=1.5.0 # Often needed for voice in discord.py, usually installed as a discord.py dependency

# Optional, but good for .env file management if you choose to use it for tokens
# python-dotenv>=0.20.0